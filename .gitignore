# segredos — NUNCA commitar
config.local.yml

# material de referência com dado pessoal de colega (nome/e-mail) — fica só local,
# não faz sentido num repositório agora público
ENC_ Notícias diárias _ 11_08_2026.eml
referencia/email-original.html
INSTRUCAO-CLAUDE-CODE-clipping-lets.md

# ambiente Python
.venv/
__pycache__/
*.pyc

# estado e log locais da coleta — regeneram sozinhos, não precisam de histórico
data/coleta.log
data/estado_rfb.json

# sistema
.DS_Store
Thumbs.db

# config local de ferramentas (Claude Code / impeccable) — específico desta máquina
.claude/settings.local.json
.impeccable/hook.cache.json
